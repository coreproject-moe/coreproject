version: 2

build:
    os: ubuntu-lts-latest
    tools:
        python: latest
    jobs:
        pre_create_environment:
            - asdf plugin add uv
            - asdf install uv latest
            - asdf global uv latest

        create_environment:
            - cd tracker/docs && uv venv

        install:
            - cd tracker/docs && uv sync

        build:
            html:
                - cd tracker/docs && uv run zensical build --clean

        post_build:
            - mkdir -p $READTHEDOCS_OUTPUT/html/
            - cd tracker/docs && cp --recursive site/* $READTHEDOCS_OUTPUT/html/
